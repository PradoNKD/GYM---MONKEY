# Carregado so por `npm run dev:mobile` (mode=mobile).
# Caminho relativo de proposito: quem atende /api e o proprio dev server, que
# encaminha pro backend em http://localhost:3000 (ver o proxy no vite.config).
# Assim o celular fala so HTTPS e nao existe CORS no caminho.
VITE_API_URL=/api
